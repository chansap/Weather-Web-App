import { DateShortnerPipe } from './date-shortner.pipe';

describe('DateShortnerPipe', () => {
  it('create an instance', () => {
    const pipe = new DateShortnerPipe();
    expect(pipe).toBeTruthy();
  });
});
